# Site-specific operational notes — live cluster addresses, device UUIDs,
# release names, node inventory. Never committed: the spec proper is written
# to be hardware- and site-agnostic (see README.md § Scope of these documents).
NOTE-a30-node.md
NOTE-*.md

# Local build/dev artifacts
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
build/
*.egg-info/

# Local cluster state / snapshots
kubeconfig
*.kubeconfig
snapshots/
.worktrees/

# Next.js build output for the monitoring UI
.next/
next-env.d.ts
*.tsbuildinfo

# Validation baselines: machine- and run-specific measurements, not spec.
baselines/

# Evaluation run artifacts. All of these are regenerated by test/evaluation/run.sh and
# test/evaluation/report.py; the measured conclusions live in specs/14-metric-evaluation.md,
# which is the durable record. The raw outputs are not.
test/evaluation/logs/
test/evaluation/__pycache__/
test/evaluation/report.json
test/evaluation/report.md
test/evaluation/phases.jsonl

# Editor workspace file: local to whoever opens the repo, not project state.
monitoring-system.code-workspace

# pytest cache
.pytest_cache/

# Implementation plans are working documents, not part of the specification.
# specs/plans/ was removed for the same reason.
docs/superpowers/
