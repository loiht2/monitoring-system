# Site-specific operational notes — live cluster addresses, device UUIDs,
# release names, node inventory. Never committed: the spec proper is written
# to be hardware- and site-agnostic (see README.md § Scope of these documents).
NOTE-a30-node.md
NOTE-*.md

# Local build/dev artifacts
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
build/
*.egg-info/

# Local cluster state / snapshots
kubeconfig
*.kubeconfig
snapshots/
.worktrees/

# Next.js build output for the monitoring UI
.next/
next-env.d.ts
*.tsbuildinfo

# Validation baselines: machine- and run-specific measurements, not spec.
baselines/

# Evaluation run artifacts. Logs are regenerated workload stdout (42 MB across 286
# files) and report.json is a machine artifact; phases.jsonl and report.md are kept
# because they are the evidence docs-internal/14 §7-8 cites.
evaluation/logs/
evaluation/__pycache__/
evaluation/report.json
